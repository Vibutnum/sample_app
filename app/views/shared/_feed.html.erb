<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items , :previous_label => '上一页'.html_safe, :next_label => '下一页'.html_safe %>
<% end %>